#!/usr/bin/env bash
# Deprecated: router prompt bodies are canonical DB content, not file fixtures
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/pv-lib.sh"

warn "pv seed-v2 is deprecated."
info "Router prompt bodies are no longer stored as markdown fixtures in the repo."
info "Canonical prompt content now lives in the Dolt database."
info "To bootstrap router prompts, clone/restore the canonical prompt-vault-db or insert/update them explicitly by exact name."
exit 1
