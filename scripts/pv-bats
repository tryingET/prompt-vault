#!/usr/bin/env bash
# pv-bats - Run bats with a repo-local TMPDIR to avoid /tmp inode exhaustion
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$SCRIPT_DIR/.."
export TMPDIR="${TMPDIR:-$REPO_DIR/.tmp-tests}"
mkdir -p "$TMPDIR"

exec bats "$@"
