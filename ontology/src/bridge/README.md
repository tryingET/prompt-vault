---
summary: "Bridge layer purpose for mapping ontology concepts to repository artifacts."
read_when:
  - "Editing concept-to-symbol mappings"
  - "Understanding ontology bridge responsibilities"
---

# Bridge

The bridge layer links ontology concepts to repo artifacts (code symbols, APIs, schemas).

- `mapping.yaml` maps stable concept IDs → repo-local symbols/paths.

