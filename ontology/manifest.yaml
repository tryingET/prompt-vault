rocs:
  layers:
    - name: core
      path: "../ontology-kernel/ontology/src"
    - name: company
      path: "../../softwareco/ontology/src"
    - name: repo
      path: "ontology/src"
  profiles:
    default: "repo-dev"
    guiding-circle:
      include_layers: ["core", "company"]
      exclude_layers: ["repo"]
      budget: 1800
    repo-dev:
      include_layers: ["core", "company", "repo"]
      budget: 2800

